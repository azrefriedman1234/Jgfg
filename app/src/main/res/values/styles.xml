<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Used by TextInputEditText in layouts -->
    <style name="PasiflonetTextInputEditText" parent="Widget.Material3.TextInputEditText.OutlinedBox">
        <item name="android:textColor">@android:color/white</item>
        <item name="android:textColorHint">@android:color/darker_gray</item>
    </style>

</resources>
